# TFM — Memoria de contexto y hoja de ruta

> Documento único de contexto para retomar el trabajo en cualquier conversación futura. Organizado como lista de tareas en orden cronológico: cada una indica su estado y toda la decisión/razón detrás, para no tener que volver a discutirla sin motivo nuevo.

## 0. Resumen del proyecto

**Título:** Evaluación de resiliencia y seguridad en orquestación de microservicios — Un análisis basado en métricas de rendimiento y aislamiento en Kubernetes.

**Proyecto software:** fork de [Online Boutique de Google](https://github.com/GoogleCloudPlatform/microservices-demo) (11 microservicios).

**Experimento central:** ejecutar un test de carga con k6 y una inyección de fallos en dos escenarios desplegados automáticamente por los pipelines:
- **Escenario A (Estándar):** sin límites de recursos ni políticas de red.
- **Escenario B (Blindado):** con todas las restricciones de seguridad y resiliencia activadas.

**Resultado esperado:** comparar cuantitativamente en Grafana el equilibrio entre el consumo/latencia que introduce la seguridad aplicada frente a la velocidad de recuperación del sistema (MTTR).

**Stack:** Azure (AKS), Terraform, GitHub Actions, Argo CD, Kubernetes, Prometheus, Grafana.

## Estado actual (para retomar rápido)

- ✅ Tarea 1 — fork incremental, no reescritura desde cero.
- ✅ Tarea 2 — commit `eef36dbb` ("chore: remove Google Cloud–specific tooling and out-of-scope service").
- ✅ Tarea 3 — 10 de 10 microservicios en alcance revisados (`productcatalogservice`, `shippingservice`, `paymentservice`, `currencyservice`, `emailservice`, `adservice`, `cartservice`, `recommendationservice`, `checkoutservice`, `frontend`; `loadgenerator` descartado, ver Tarea 12). Ver tabla de estado y checklist en Tarea 3.
- ⏳ Tareas 4-14 — diseñadas, nada implementado todavía.
- **Rama de trabajo:** todo el trabajo de la Tarea 3 en adelante vive en `refactor/local-microservice-validation`, no en `main`. `main` se mantiene en `b9a978db` ("Add GKE labels") hasta fusionar vía PR.
- **Orden de ejecución:** primero todo lo que no cuesta dinero — Tareas 3-5 (validación local: microservicios, `kind`, Terraform) y Tarea 6 (CI, que no toca Azure salvo credenciales de solo lectura). El primer `terraform apply` real contra Azure (Tareas 10-11) va al final, no al principio.

---

## Tarea 1 — Método de trabajo: fork incremental ✅

**Decisión:** seguir modificando este mismo fork de forma incremental. No copiar solo `src/` a un repo nuevo para reconstruir desde cero.

**Razones:**
1. El 90% del "software project" que pide el enunciado ya está aquí y es reutilizable: manifiestos Kubernetes de los 11 microservicios, `kustomize/base` + `kustomize/components/network-policies/` (exactamente el aislamiento de red del Escenario B). Reescribirlo a mano es trabajo mecánico sin valor para el TFM y con riesgo de bugs.
2. El historial de git es evidencia de proceso citable en la memoria.
3. Menor riesgo de quedarse a medias — cada pieza se prueba según se añade/borra.
4. La única razón real para reescribir sería el ejercicio de aprender a escribir YAML de Kubernetes desde cero, y eso no es lo que evalúa el TFM.

---

## Tarea 2 — Auditar y limpiar el fork ✅ (commit `eef36dbb`, rama `refactor/local-microservice-validation`)

### Borrado definitivamente (56 rutas, sin ningún valor ni como ejemplo)

- **Tooling de despliegue exclusivo de Google Cloud**: `.deploystack/` (Cloud Shell, usa Secret Manager y un proyecto GCP privado), `cloudbuild.yaml` (Google Cloud Build).
- **`skaffold.yaml`** — herramienta de bucle de desarrollo local, sustituida por el pipeline de CI/CD; además referenciaba servicios ya borrados.
- **`istio-manifests/`** — duplicado literal y obsoleto de `kustomize/components/service-mesh-istio/` (que sí se conserva).
- **Documentación del tooling borrado**: `docs/deploystack.md`, `docs/cloudshell-tutorial.md`.
- **Proceso de release oficial del proyecto de Google**: `docs/releasing/` (scripts que hacen build+push a su Artifact Registry y cortan releases del repo oficial), `release/` (artefactos generados por esos scripts, duplicados de `kubernetes-manifests/`+`istio-manifests/`), `.github/workflows/make-release.yaml`.
- **CI/CD hardcodeada al proyecto GCP privado de Google (`online-boutique-ci`)**: `.github/workflows/ci-main.yaml`, `deploy-pr.yaml`, `cleanup.yaml` (mecanismo de *entorno efímero por PR*, descartado también por decisión propia — ver "Decisiones descartadas explícitamente" al final del documento), `.github/terraform/` (provisiona el clúster GKE compartido que usaban esos workflows), `.github/release-cluster/` (Ingress exclusivo de GKE: `BackendConfig`, certificado gestionado de Google, sin equivalente en AKS).
- **Gobernanza de proyecto open source** (es un fork académico, no un proyecto que reciba contribuciones externas): `.github/auto-approve.yml`, `CODE_OF_CONDUCT.md`, `CODEOWNERS`, `CONTRIBUTING.md`, `header-checker-lint.yml`, `ISSUE_TEMPLATE/`, `pull_request_template.md`, `SECURITY.md`, `snippet-bot.yml`.
- **`src/shoppingassistantservice/` + `kustomize/components/shopping-assistant/`** — 12º microservicio opcional (asistente de compra con Vertex AI/Gemini), fuera del alcance declarado de "11 microservicios", ya excluido por defecto en `kubernetes-manifests/kustomization.yaml`/`kustomize/base`.

### Mantenido a propósito aunque no se vaya a usar directamente

- **`terraform/`** (GCP/GKE actual) — se reemplaza por Terraform de Azure, pero se deja como referencia estructural (`variables.tf`/`outputs.tf`/`providers.tf`) hasta que se escriba el nuevo.
- **`helm-chart/`** — chart de Helm para desplegar la propia app Online Boutique (no confundir con el CLI `helm`, ver Tarea 13). No se usa porque el despliegue es vía Kustomize + Argo CD, pero es agnóstico de nube y no cuesta nada mantenerlo.
- **`kustomize/components/service-mesh-istio/`** y demás componentes opcionales de Kustomize (`alloydb`, `spanner`, `memorystore`, `google-cloud-operations`, `cymbal-branding`, `single-shared-session`, `custom-base-url`, `non-public-frontend`, `container-images-registry/tag/tag-suffix`, `without-loadgenerator`) — están comentados/opt-in por defecto, no interfieren con nada, y sirven de ejemplo de cómo añadir componentes a los overlays propios.
- **`src/loadgenerator/`** (Locust) y `kubernetes-manifests/loadgenerator.yaml` — se sustituye por k6, pero el `locustfile.py` sirve de referencia de los "user journeys" (navegar/carrito/comprar) al escribir el script de k6, incluido el perfil de "usuario testigo". Borrar solo cuando el script de k6 ya esté listo.
- **`.github/workflows/{kubevious-manifests-ci,helm-chart-ci,kustomize-build-ci,terraform-validate-ci}.yaml`** — no dependen de credenciales GCP. `kustomize-build-ci` y `terraform-validate-ci` de hecho coinciden exactamente con checks ya planeados en la Tarea 6 (validan por ruta, no por contenido, así que siguen funcionando cuando se sustituya el contenido de `terraform/`).
- **`.github/renovate.json5`** — sigue actualizando dependencias de los 11 microservicios (evidenciado por commits recientes de Renovate en el historial), no está atado a GCP.

---

## Tarea 3 — Validación local de microservicios ✅

**Objetivo:** dejar cada uno de los 11 microservicios listo para producción en local (sin código GCP-específico, con tests, con linter, con Dockerfile endurecido) antes del primer `terraform apply` contra Azure — el único paso de todo el plan que cuesta dinero real. El clúster local (`kind`, Tarea 4) y Terraform (Tarea 5) son tareas propias porque necesitan que los 11 microservicios ya estén revisados.

### Reglas a aplicar en cada microservicio

1. Instala dependencias y compila/arranca sin error.
2. Elimina Stackdriver Profiler si existe (`cloud.google.com/go/profiler`, `@google-cloud/profiler`, `googlecloudprofiler`) — no hay equivalente necesario, Prometheus/Grafana (Tarea 14) no hace profiling de función. Elimina también las variables de entorno asociadas (`ENABLE_PROFILER`/`DISABLE_PROFILER`) del código; la limpieza en los manifiestos de Kubernetes se hace en la Tarea 8, no aquí.
3. Elimina OpenTelemetry si hay una implementación real (no un stub muerto): no hay ni habrá un OTel Collector en la infraestructura de este TFM. Trazado distribuido y métricas son señales de observabilidad distintas — lo que pide la Tarea 14 es la segunda, no la primera. Un stub sin efecto real se trata como código muerto (regla 4).
4. Elimina código muerto propio del microservicio (sin relación con GCP): clases/funciones nunca instanciadas, ramas condicionales cuyas dos salidas hacen lo mismo, imports heredados de versiones antiguas del lenguaje, variables de entorno que ya no lee nadie, ficheros que no compilan/ejecutan (dependencias no declaradas, paquetes obsoletos).
5. Elimina cualquier otro código GCP-específico que no sirva al stack declarado (Azure/Terraform/Kubernetes/Prometheus/Grafana) — evaluar caso por caso, no borrar por sistema.
6. Escribe los tests que falten. Cubre solo lógica de negocio real y alcanzable sin red (parseo, cálculo, filtrado, template rendering) — no montar un servidor gRPC real en el test. Todos los tests (existentes y nuevos) en verde antes de abrir PR.
7. Corre el linter en local con la misma herramienta que correrá en CI (Tarea 6). Configuración del linter siempre dentro del propio microservicio, nunca compartida en la raíz del repo.
8. Dockerfile de producción:
   - Imagen base fijada por digest exacto (`@sha256:...`), nunca por tag.
   - Imagen final mínima: no debe arrastrar compiladores/herramientas de build (`npm`, `pip`, `gcc`) del stage de builder.
   - Usuario no-root explícito (`nonroot` de distroless en Go; creado a mano con `addgroup`/`adduser` en Alpine para Node/Python).
   - Todo `COPY`/`COPY --from=builder` al stage final lleva `--chown` explícito al usuario no-root, incluida la copia del binario/runtime en sí, no solo el código de la app — no depender de un permiso por defecto en vez de una propiedad explícita.
9. `Dockerfile.dev` con hot-reload si se prevé iterar activamente sobre ese servicio: `air` (Go), `nodemon` vía `npx` como `devDependency` (Node, nunca instalado globalmente), `watchdog`/`watchmedo auto-restart` (Python).
10. Añade el servicio a `compose.yaml` (build con `Dockerfile.dev`, puerto de host libre — remapear si el puerto interno por defecto choca con otro servicio ya añadido, sin tocar el puerto interno). Añade también `healthcheck` (para que `docker compose up <servicio>` levante también sus dependencias reales, no solo en orden de creación) y `depends_on: <dependencia>: condition: service_healthy` según las variables `*_SERVICE_ADDR` que consuma — sin tocar ningún `Dockerfile.dev` para ello: BusyBox `nc` ya viene de fábrica en las imágenes Alpine (Go/Node/Python/Java) y el `bash` con `/dev/tcp` ya viene en la imagen Debian de .NET SDK, suficiente para un chequeo TCP sin instalar nada nuevo; para servicios HTTP (`frontend`) basta con `wget --spider` sobre su endpoint de salud, también ya presente.

**Ejecución de todo lo anterior: siempre dentro de contenedores Docker oficiales por lenguaje (`golang:1.27-alpine`, `node:20-alpine`, etc.), igual en local que en CI.** Evita depender de una action `setup-<lenguaje>` distinta por tecnología y reduce superficie de cadena de suministro. Genera la salida en el formato que cada action de reporting de la Tarea 6 espera (JUnit XML, LCOV, `go test -json`...) y pásasela a esa action; no sustituye a esas actions nativas de reporting.

### Estado por microservicio

| Microservicio | Revisado | Tests | Linter | Dockerfile prod | Dockerfile.dev |
|---|---|---|---|---|---|
| `productcatalogservice` (Go) | ✅ | ✅ (ya tenía) | golangci-lint | ✅ | ✅ (`air`) |
| `shippingservice` (Go) | ✅ | ✅ (ya tenía) | golangci-lint | ✅ | ✅ (`air`) |
| `paymentservice` (Node) | ✅ | ✅ (nuevo, Jest) | ESLint | ✅ | ✅ (`nodemon`) |
| `currencyservice` (Node) | ✅ | ✅ (nuevo, Jest) | ESLint | ✅ | ✅ (`nodemon`) |
| `emailservice` (Python) | ✅ | ✅ (nuevo, pytest) | ruff | ✅ | ✅ (`watchdog`) |
| `adservice` (Java) | ✅ | ✅ (nuevo, JUnit 5) | Checkstyle | ✅ | ✅ (`watchexec --poll`, ver nota) |
| `cartservice` (C#) | ✅ | ✅ (ya tenía) | `dotnet format` | ✅ | ✅ (`dotnet watch`) |
| `recommendationservice` (Python) | ✅ | ✅ (nuevo, pytest) | ruff | ✅ | ✅ (`watchdog`) |
| `checkoutservice` (Go) | ✅ | ✅ (ya tenía) | golangci-lint | ✅ | ✅ (`air`) |
| `frontend` (Go) | ✅ | ✅ (ya tenía + nuevo, `handlers_test.go`) | golangci-lint | ✅ | ✅ (`air`) |
| `loadgenerator` | descartado — se sustituye por k6, ver Tarea 12 |||||

Nota: `productcatalogservice`, `shippingservice`, `paymentservice`, `emailservice`, `adservice`, `cartservice` y `recommendationservice` dejan a propósito algún hallazgo de estilo sin corregir en el linter, como demostración de que la herramienta funciona — no es descuido si aparece en su salida. En `productcatalogservice`: `if (os.Getenv("PORT") != "") == true` en `server.go` (regla `S1002` de `staticcheck`, comparación redundante contra constante booleana) — mismo tipo de hallazgo trivial que en `shippingservice`, introducido tras corregir el `errcheck` real de `srv.Serve` (ver nota más arriba). En `shippingservice`: `if (count == 0) == true` en `quote.go` (regla `S1002` de `staticcheck`, comparación redundante contra constante booleana) — hallazgo trivial introducido a propósito para verificar que el comando de linting del README funciona, mismo propósito demostrativo que el resto de esta nota (sustituye al `ok == true` de `main.go` de una revisión anterior, ya corregido). En `paymentservice`: `` `...expired on ${month}\/${year}` `` en `charge.js` (regla `no-useless-escape` de ESLint, escapar `/` dentro de un template literal no hace nada — la barra no es un carácter especial ahí, a diferencia de dentro de un literal de regex) — mismo propósito demostrativo, introducido tras limpiar los 4 hallazgos reales de `no-unused-vars` que ya traía el servicio. En `emailservice`: `logger.info(f'starting the email service in dummy mode.')` en `email_server.py` (regla `F541` de ruff, f-string sin ningún placeholder — la `f` sobra), introducido tras corregir el único hallazgo real (`UP032`, `.format()` en `email_client.py`). En `adservice`: `if (server != null) server.awaitTermination();` sin llaves en `blockUntilShutdown()` (regla `NeedBraces`). En `cartservice`: espacio de más antes del paréntesis en `Console.WriteLine ("Checking CartService Health")` (`HealthCheckService.cs`, categoría `WHITESPACE` de `dotnet format`). En `recommendationservice`: `import os, random` en una sola línea en `recommendation_server.py` (regla `E401`, del conjunto por defecto de ruff — este proyecto no usa `ruff.toml`, así que el hallazgo tiene que salir de ahí).

### Excepciones y hallazgos puntuales (no generalizables al resto de microservicios)

- `productcatalogservice` tenía una ruta alternativa de carga de catálogo vía AlloyDB + Secret Manager, nunca activada (variable de entorno nunca seteada en ningún manifiesto) — eliminada en vez de migrada a Azure (regla 5).
- `productcatalogservice`: `catalog_loader.go` usaba `github.com/golang/protobuf/jsonpb` — hallazgo real de `staticcheck` (regla `SA1019`, paquete deprecado a favor de `google.golang.org/protobuf/encoding/protojson`), se corrigió por ser un cambio de una línea sin riesgo (misma firma, `[]byte` en vez de `io.Reader`, así que de paso se quitó el import `bytes` ya innecesario). `go mod tidy` confirmó que `github.com/golang/protobuf` ya no hacía falta y lo quitó del `go.mod` (sin cambios en `go.sum`, esa dependencia no tenía entradas propias que limpiar ahí).
- `productcatalogservice`: **`go srv.Serve(listener)` sin comprobar su valor de retorno en `server.go` (regla `errcheck`) — decisión revisada, ya corregido.** Se dejó sin resolver en la revisión inicial de la Tarea 3 (se llama en una goroutine aparte para no bloquear, sin un sitio obvio donde propagar el error), pero tiene arreglo real y sencillo: envolverlo en una función anónima que haga `log.Fatalf` si `Serve` termina con error, en vez de descartarlo en silencio — mismo patrón que el manejo de errores de arranque en `shippingservice`/`checkoutservice`. Verificado en vivo: `golangci-lint run ./...` pasa a `0 issues`, build+tests en verde, y el smoke test de producción (`docker build`+`docker run`+`grpcurl ListProducts`) sigue respondiendo igual. `go.mod`/`go.sum` actualizados de paso con `go get -u ./... && go mod tidy` (`grpc` v1.83.2→v1.84.0, toolchain `go1.27.1`) — quitó del todo las indirectas de OpenTelemetry que antes arrastraba `grpc` como transitivas, ya no hacen falta con la versión nueva.
- **`productcatalogservice`: `Dockerfile.dev` con `go install github.com/air-verse/air@latest` fijado a `@v1.67.4`** (última release estable en GitHub en el momento de revisar) — reproducibilidad del ciclo de hot-reload entre máquinas/días distintos, mismo motivo que fijar por digest en producción aunque aquí baste con un tag de versión (no es la imagen final). Deliberadamente **no** aplicado al resto de `Dockerfile.dev` de Go (`shippingservice`, `checkoutservice`, `frontend`) — quedan en `@latest`, decisión explícita del usuario de no tocarlos sin pedirlo.
- `productcatalogservice`: **`golangci-lint` v2 eliminó el flag `--out-format` que usaba antes el comando del README** (`golangci-lint --version` → 2.13.2) — los formatos ahora son `--output.<formato>.path` (`text`, `json`, `checkstyle`, `sarif`, etc., sin `github-actions` entre ellos). No hace falta generar un fichero de informe para `golangci-lint-action`: a diferencia de `dorny/test-reporter`/`gwatts/go-coverage-action`, esa action ejecuta el linter ella misma dentro del job y anota la PR de forma nativa (ver paso 5 de la Tarea 6) — el comando del README para reproducir en local se simplificó a `golangci-lint run ./...` sin flags de formato. Aplicable a los demás microservicios Go (`shippingservice`, `checkoutservice`, `frontend`) si sus README llegaran a usar el flag viejo.
- `productcatalogservice`: **tests nuevos en `catalog_loader_test.go` (revisado, sube la cobertura del 30.8% original al 51.5%)** — cubre `loadCatalog`/`loadCatalogFromLocalFile` (0%→100%/84.6%: parseo real del `products.json` del propio repo, más el caso de error cuando el fichero no existe, usando `t.Chdir` a un directorio temporal — solo I/O de fichero local y parseo JSON, sin red, entra de lleno en la regla 6) y la rama de recarga de `parseCatalog` (40%→100%: catálogo vacío, `reloadCatalog=true`, y el camino de error cuando `loadCatalog` falla). Queda sin cubrir solo el error de `protojson.Unmarshal` dentro de `loadCatalogFromLocalFile` (JSON malformado) — fabricar un `products.json` corrupto solo para ese 15% no aporta señal real. **Umbral de cobertura en CI fijado al 45%, no el 70% general** (ver gate de cobertura en la Tarea 6). Cobertura real medida: 51.5% (`go tool cover -func`, `genproto/` excluido) — `main`/`run`/`init` (arranque del servidor) y `Check`/`Watch` (health check gRPC) se quedan a 0% porque cubrirlos exigiría montar un servidor gRPC real en el test, prohibido por la regla 6. 45% en vez del 51.5% exacto para dejar colchón ante pequeñas regresiones futuras sin romper el CI. Decisión: bajar el umbral para este servicio, no excluir `server.go` del scope de cobertura — a diferencia de `genproto/` (código generado, sin señal de riesgo real), es un fichero propio y excluirlo del cómputo solo por ser difícil de testear sentaría un precedente para inflar el % en cualquier otro sitio.
- `paymentservice`: **`collectCoverageFrom` añadido al `jest` de `package.json` (sin él, Jest solo instrumentaba `charge.js`** — el único fichero que `charge.test.js` importaba —, **así que el `100%` que salía antes no reflejaba el servicio real, mismo problema ya documentado para `cartservice`).** Con `["*.js", "!*.test.js", "!eslint.config.js"]` entran también `index.js`, `logger.js` y `server.js`. De paso, **tests nuevos en `server.test.js`** para las partes de `server.js` con lógica real y alcanzable sin levantar un servidor de red (regla 6): `CheckHandler` (responde `SERVING`), `ChargeServiceHandler` (delega en `charge()` y traslada éxito/error al callback) y la construcción de `HipsterShopServer` (carga real de `demo.proto`/`health.proto` con los ficheros ya presentes en `proto/`, registra ambos servicios — no toca red, solo `listen()` la abre y esa sí queda sin testear). Cobertura real medida: 84.12% (`charge.js`/`logger.js` al 100%, `server.js` al 85.71% con solo `listen()` sin cubrir, `index.js` a 0% porque su único efecto es llamar a `server.listen()` al cargarse, mismo motivo de red que excluye `main`/`init` en los Go). Por encima del 70% general de la Tarea 6, así que **sin excepción de umbral** — a diferencia de `productcatalogservice`/`shippingservice`, no hace falta documentar aquí un umbral distinto.
- `shippingservice`: **umbral de cobertura en CI fijado al 55%, no el 70% general** (ver gate de cobertura en la Tarea 6). Cobertura real medida: 63.0% (`go tool cover -func`, `genproto/` excluido) — `GetQuote`/`ShipOrder`/`quote.go`/`tracker.go` al 100% (lógica de negocio real, cubierta), y `main`/`init` (arranque del servidor) y `Check`/`Watch` (health check gRPC) a 0%, mismo motivo que en `productcatalogservice` (exigiría un servidor gRPC real en el test, prohibido por la regla 6). 55% en vez del 63.0% exacto para dejar colchón, mismo criterio que el 25% de `productcatalogservice`.
- `paymentservice`: **`devDependencies` desactualizadas — `eslint` 9.39.5 ("no longer supported"), `jest-junit` 16 (arrastraba `uuid@8.3.2`, deprecado) y `jest` 29 (arrastraba `glob@7.2.3`/`inflight@1.0.6`, deprecados con aviso de fuga de memoria)**, confirmado en vivo por los `npm warn deprecated` de `npm ci`. Subidas a `eslint@^10.0.0` (+ `@eslint/js@^10.0.0`, `globals@^17.0.0`, misma mayor para mantener compatibilidad de flat config), `jest-junit@^17.0.0` (quita `uuid@8.3.2`) y `jest@^30.0.0` (quita `glob@7.2.3`/`inflight@1.0.6` de la superficie directa) — `package-lock.json` regenerado. Verificado que no rompe nada: mismos 5 tests en verde, misma cobertura 100%, mismos 4 hallazgos de ESLint (`no-unused-vars`, sin relación). **Aviso residual de `glob@10.5.0` deprecado — probado y descartado un fix con `overrides`, no se aplica finalmente (decisión revisada).** Viene de `test-exclude@7.0.2` (usado por `babel-plugin-istanbul@8.0.0`, dependencia transitiva interna de `jest@30.5.2` para el cálculo de cobertura), que pide `glob@^10.2.0`; `test-exclude@8.0.0` ya está migrado a `glob@^13.0.6` sin avisos, pero `babel-plugin-istanbul@8.0.0` todavía lo fija a `^7.0.1`, así que `npm install` normal nunca lo elige. Se probó forzarlo con el campo `overrides` de `package.json` (`"glob": "^13.0.6"`) — funcionaba (verificado: `npm ls glob` con una sola versión en todo el árbol, sin aviso, mismos tests/cobertura/lint) pero se descartó a petición expresa: no meter `overrides` en `package.json` solo para silenciar un aviso de una dependencia transitiva interna de una `devDependency`, cuando el propio Jest lo resolverá en una futura versión sin necesidad de mantener ese override a mano. No es una dependencia de producción (`Dockerfile` no instala `devDependencies`), así que se deja documentado como ruido conocido, no como algo pendiente de arreglar. Todas devDependencies, sin impacto en producción (`Dockerfile` hace `npm install --only=production`, nunca las instala).
- `paymentservice`: **`npm audit` real, a diferencia del aviso de `glob` de arriba, este sí en una dependencia de producción — `protobufjs@7.5.9` (transitiva de `@grpc/proto-loader@0.8.1`, dependencia de producción real, usada por `server.js` para cargar `demo.proto`) con una vulnerabilidad de severidad alta (DoS por expansión `Any` sin límite, parseo de opciones `.proto` en bucle infinito).** `npm audit fix` la resolvió sin `--force`: `protobufjs` subió a `7.6.6`, dentro del mismo rango `^7.5.5` que ya declaraba `@grpc/proto-loader` en su propio `package.json` — no fue necesario tocar `package.json` de `paymentservice` ni forzar una mayor incompatible, solo regenerar `package-lock.json`. `npm audit` queda en 0 vulnerabilidades. Verificado que no rompe nada: mismos 5 tests, misma cobertura 100%, mismos 4 hallazgos de ESLint, y `docker compose up`/`docker build` de producción arrancan igual.
- `paymentservice`: `server.js` llamaba a `server.start()` justo después de `server.bindAsync(...)` — `@grpc/grpc-js` deprecó ese método (`bindAsync` ya arranca el servidor por sí solo desde hace varias versiones), generaba un `DeprecationWarning` real en cada arranque, confirmado en vivo (`docker compose up paymentservice`). Código muerto (regla 4): eliminada la llamada, sin efecto en el comportamiento (`Charge` sigue respondiendo igual, verificado con `grpcurl`).
- `currencyservice` y `paymentservice` tenían una implementación real de OpenTelemetry (no un stub) que exportaba a un `COLLECTOR_SERVICE_ADDR` inexistente en la infraestructura — eliminada por la regla 3. `currencyservice` la registraba de forma incondicional (sin depender de `ENABLE_TRACING`).
- `currencyservice/client.js` no era código de ejemplo válido: usaba un paquete no declarado (`grpc`, obsoleto, pre-`grpc-js`) y una librería de trazas anterior a OpenTelemetry — eliminado entero (regla 4: comprobar que un fichero compila/ejecuta antes de conservarlo como ejemplo).
- Tests de conversión de divisas con decimales (`currencyservice`): comparar con tolerancia, no con igualdad exacta — imprecisión de coma flotante real del algoritmo, no un bug.
- Si Jest falla al parsear una dependencia (error de resolución de módulos, no de estilo), comprobar primero si es un paquete ESM-only (p. ej. `uuid`) antes de tocar la configuración de Jest — normalmente hay un reemplazo nativo (`crypto.randomUUID()`).
- Si un microservicio renderiza un template (Jinja2 o similar) con campos de un `.proto`, comprobar que los nombres de campo existen en el mensaje actual — Jinja2 no falla con variables inexistentes por defecto (sin `StrictUndefined`), el bug queda silencioso (caso real: `emailservice`, `street_address_1`/`_2` ya no existen, solo `street_address`).
- Dependencias de test siempre en un fichero separado del de producción (`requirements-test.in` en Python) — nunca en `requirements.in`/`package.json` de producción.
- **Regla recurrente: el código generado por protobuf/gRPC no debe contar ni en el linter ni en la cobertura.** En Python (`emailservice`) el linter se excluye vía flag CLI (`ruff check . --exclude *_pb2.py,*_pb2_grpc.py`); en Java (`adservice`) Checkstyle no tiene flag CLI de exclusión, así que se acota directamente la tarea a la carpeta de código propio (`checkstyleMain.source = fileTree("src/main/java")`). En C# (`cartservice`) el propio `dotnet format` ya ignora `obj/` de fábrica (no hizo falta excluir nada ahí) — pero la **cobertura sí se contamina**: `dotnet test --collect:"XPlat Code Coverage"` instrumenta y cuenta el código generado en `obj/**/protos/*.cs` igualmente, porque Coverlet trabaja a nivel de binario compilado, no de "qué es código fuente del proyecto" — el 47% que reportaba antes de excluirlo no significaba nada real (ver nota de `cartservice` más abajo). En Go (`checkoutservice`, y por extensión `productcatalogservice`/`shippingservice`, con la misma estructura de carpeta `genproto/`) pasa lo mismo: `go test -coverprofile=... ./...` también instrumenta y lista a 0% cada función generada de `genproto/`, contaminando el `go tool cover -func` con decenas de líneas irrelevantes — corregido acotando los paquetes cubiertos con `go test -coverprofile=coverage.out $(go list ./... | grep -v /genproto)` (ver nota de `checkoutservice` más abajo; aplicado también, retroactivamente, a los README de `productcatalogservice` y `shippingservice`, que tenían el mismo comando sin filtrar desde antes de que se detectara el patrón).
- `paymentservice`: **puerto interno cambiado de `50051` a `50052`** (decisión revisada) — `50051` ya lo usa `shippingservice`, y como `compose.yaml` levanta los 11 microservicios de desarrollo a la vez en el mismo host, un simple remapeo de puerto de host (`50052:50051`, manteniendo el interno) obligaba a que desarrollo y producción expusieran puertos distintos hacia fuera. Se prefiere que `paymentservice` tenga el mismo puerto en desarrollo y en producción: cambiado el propio puerto interno de la app (`PORT`, `EXPOSE` del Dockerfile, `containerPort`/`readinessProbe`/`livenessProbe`/`Service.port`/`targetPort` en `kubernetes-manifests/paymentservice.yaml` y `kustomize/base/paymentservice.yaml`, `port` en `kustomize/components/network-policies/network-policy-paymentservice.yaml`, y el `PAYMENT_SERVICE_ADDR` de `checkoutservice` en los tres sitios que lo referencian) — sin tocar `shippingservice`, que conserva `50051`. Aplicado también a `helm-chart/` por consistencia aunque no se use (ver Tarea 2). `index.js` no tenía un puerto por defecto hardcodeado (`PORT` siempre viene del entorno), así que el cambio es puramente de configuración, no de código.
- **Mismo criterio aplicado a `recommendationservice` (`8080`→`8081`) y `frontend` (`8080`→`8082`)** — ambos, junto con `emailservice`, competían por el puerto interno `8080` por defecto, así que `compose.yaml` ya los remapeaba solo por fuera (`8081:8080`/`8082:8080`), con el mismo desajuste dev/prod que `paymentservice`. Cambiado el puerto interno real (no solo el remapeo de host) en: `PORT`/`EXPOSE` del `Dockerfile`, `containerPort`/`readinessProbe`/`livenessProbe`/`Service.port`/`targetPort` en `kubernetes-manifests/`, `kustomize/base/` y `helm-chart/templates/` de cada servicio, `port` en `kustomize/components/network-policies/network-policy-recommendationservice.yaml` (frontend no tiene puerto en la suya, es `ingress: [{}]` abierto), y el valor por defecto hardcodeado en código (`recommendation_server.py`/`client.py` para `recommendationservice`, `main.go` para `frontend`, a diferencia de `paymentservice` que no tenía default hardcodeado). `RECOMMENDATION_SERVICE_ADDR` actualizado a `:8081` en los tres sitios donde `frontend` lo referencia (`kubernetes-manifests/`, `kustomize/base/`, `helm-chart/templates/`, más `compose.yaml`). `emailservice` conserva `8080` sin tocar, igual que `shippingservice` conservó `50051` en el caso de `paymentservice`.
- `adservice`: `io.grpc:grpc-census` declarado en `build.gradle` sin usarse en ningún sitio (Census es OpenCensus, precursor de OpenTelemetry) — eliminado (regla 5). Imports muertos en `AdService.java`, nunca usados — eliminados (regla 4): `ImmutableMap`, y `TimeUnit` (este último detectado por Checkstyle una vez corregido para no lintar el código generado, no a simple vista).
- `adservice`: `io.grpc.services.HealthStatusManager` está deprecado por el propio grpc-java a favor de `io.grpc.protobuf.services.HealthStatusManager` — encontrado al añadir `-Xlint:deprecation` al compilador (ya permanente en `build.gradle`, no solo para este diagnóstico) tras ver el aviso "uses or overrides a deprecated API" en un build normal. De paso se cambió el `import io.grpc.services.*` (wildcard) por el import explícito de la clase nueva — único símbolo que se usaba de ese wildcard.
- `adservice`: `log4j2.xml` traía claves JSON pensadas para que el agente de Stackdriver Logging las interprete de forma especial (`logging.googleapis.com/trace`, `.../spanId`, `.../traceSampled`) — nunca se rellenaban (el trazado real nunca se implementó, `initTracing`/`initStats` eran TODOs muertos como en `shippingservice`). Se quitan esas tres claves y se conserva el resto del `JsonLayout` (logging estructurado en sí es buena práctica, independiente de nube).
- `adservice`: el Dockerfile de producción copiaba `/app` entero del builder (código fuente, wrapper de Gradle, caché) al stage final — no arrastraba compiladores, pero sí mucho más de lo necesario para ejecutar. Corregido a copiar solo `build/install/hipstershop` (la distribución ya instalada). `cartservice` ya seguía este mismo patrón de fábrica (`dotnet publish -o /cartservice` + `COPY --from=builder /cartservice .`, nunca copió el `/app` del builder entero) — confirma que es el patrón correcto para cualquier lenguaje con un paso de "instalar/publicar" antes de la imagen final.
- `adservice`: para que `AdServiceTest` pueda instanciar `AdServiceImpl` sin arrancar un servidor gRPC real, se cambió su visibilidad de `private` a paquete (sin modificador) — mismo criterio que los `module.exports` añadidos en los servicios Node, cambio mínimo solo para habilitar el test.
- `adservice`: requiere fijar `application { mainClass = 'hipstershop.AdService' }` en `build.gradle` para poder usar `gradlew run` en desarrollo — no existía porque el Dockerfile de producción usa sus propios *start scripts* (`adService`/`adServiceClient`, con `startScripts.enabled = false`). Los dos mecanismos conviven sin conflicto.
- `cartservice`: `AlloyDBCartStore.cs` y `SpannerCartStore.cs` eliminados enteros — dos backends de base de datos GCP-específicos (AlloyDB, Cloud Spanner), nunca seleccionados en este proyecto (`kubernetes-manifests/cartservice.yaml` solo setea `REDIS_ADDR`, nunca `SPANNER_*`/`ALLOYDB_*`) — mismo patrón que el AlloyDB muerto de `productcatalogservice` (regla 5). `AlloyDBCartStore.cs` además usaba `Google.Cloud.SecretManager.V1` para leer la contraseña de la base de datos y montaba las queries SQL con interpolación de string directa (inyección SQL) — no se corrigió el bug porque el fichero entero se elimina, no se conserva. Dependencias NuGet `Google.Cloud.Spanner.Data`, `Npgsql` y `Google.Cloud.SecretManager.V1` eliminadas de `cartservice.csproj` junto con ellos. `Startup.cs` simplificado a solo Redis (configurado) vs. in-memory (fallback), quitando las ramas `else if` de Spanner/AlloyDB.
- `cartservice`: el Dockerfile de producción ya venía bastante bien (digest fijado, imagen final `runtime-deps:...-chiseled` — ya mínima, sin ni siquiera shell — y `USER 1000` ya no-root) — solo le faltaba `--chown=1000:1000` explícito en el `COPY --from=builder`, igual que al resto (regla 8). Los `COPY` en una imagen *chiseled* (sin `/etc/passwd`) sí aceptan UID:GID numérico sin problema, no hace falta un usuario con nombre.
- `cartservice`: **`Dockerfile.debug` (imagen de depuración remota con `procps`/`unzip`/`wget`, sin relación con hot-reload) sustituido por el `Dockerfile.dev` estándar con `dotnet watch run`.** A diferencia de Gradle, aquí no hizo falta descubrir el problema por prueba y error: .NET documenta oficialmente que su vigilancia de ficheros no es fiable en Docker y publica la solución (`DOTNET_USE_POLLING_FILE_WATCHER=1`, sondeo en vez de eventos nativos) — se aplicó directamente sin necesidad de reproducir el fallo primero.
- `cartservice`: `.editorconfig` para `dotnet format` vive en la **raíz del repo**, no dentro del microservicio — excepción deliberada a la regla de configs por microservicio: EditorConfig es un estándar pensado para ser único por repositorio (`root = true`, cascada a todos los lenguajes), ya existía de fábrica cubriendo los 5 lenguajes del proyecto, y moverlo rompería esa convención sin aportar nada. Distinto de ESLint/ruff/Checkstyle, que sí son configuración específica de una herramienta de un microservicio concreto.
- `cartservice`: el informe de cobertura (`coverage.cobertura.xml`) sin excluir nada reportaba 46.69% — número sin sentido, dominado por clases de protobuf/gRPC generadas (`CartItem`, `AddItemRequest`, `Cart`, `CartServiceBase`...) que nadie escribe a mano. El código propio real: `CartService` 100%, `Startup` 69%, `RedisCartStore` 36%, `HealthCheckService` 0% (ningún test llama al health check). Arreglado con `tests/coverlet.runsettings` (`ExcludeByFile: **/obj/**/*.cs`), pasado con `--settings` en el comando de cobertura del README.
- `cartservice`: faltaba `coverlet.collector` en `tests/cartservice.tests.csproj` — sin él, `dotnet test --collect:"XPlat Code Coverage"` no genera ningún `coverage.cobertura.xml` (falla en silencio, sin error). Añadido. De paso, `TestResults/` (carpeta que genera ese comando, con un GUID por ejecución) no estaba en el `.gitignore` raíz — el mismo tipo de hueco que ya se corrigió para `coverage.out`/`coverage.html` de Go, esta vez para .NET.
- `cartservice`: dos `using` muertos en `Startup.cs` — `Microsoft.AspNetCore.Diagnostics.HealthChecks` y `Microsoft.Extensions.Diagnostics.HealthChecks` (regla 4). El health check real es la clase gRPC propia (`HealthCheckService`), no el framework de health checks de ASP.NET Core — ningún símbolo de esos dos namespaces se usaba. Eliminados a mano: `dotnet format` no los detecta porque la regla que los pillaría (`IDE0005`) viene con severidad `silent` por defecto en los analizadores de .NET, y se descartó activarla solo para esto — mantener el linter con su comportamiento de fábrica, sin reglas añadidas a propósito.
- `emailservice`: **bug real del código original destapado al escribir tests — `Watch` devolvía `HealthCheckResponse.UNIMPLEMENTED`, un valor que no existe en `health.proto`** (solo `UNKNOWN`/`SERVING`/`NOT_SERVING`/`SERVICE_UNKNOWN`), así que cualquier llamada a `Watch` lanzaba `AttributeError`. Corregido a `context.abort(grpc.StatusCode.UNIMPLEMENTED, ...)`, la forma correcta de declarar un RPC no implementado. Segundo bug real en `templates/confirmation.html`: un espacio de más tras el punto decimal del coste de envío (`5. 99 USD` en vez de `5.99 USD`) que los tests originales no detectaban porque solo comprobaban el precio de los artículos — corregido. Tests nuevos para `Check`/`Watch`/`SendOrderConfirmation`, `CustomJsonFormatter` y la plantilla (coste de envío, relleno de céntimos a dos dígitos, autoescape de HTML en campos del pedido); cobertura acotada por CLI a código propio (`--cov=email_server --cov=logger`, fuera `demo_pb2*` y el propio test): 74% (`logger.py` 100%, `email_server.py` 61% con solo `start()`/`__main__` sin cubrir, regla 6), por encima del 70% general, sin excepción de umbral. Dependencias subidas (`grpcio`/`grpcio-health-checking` 1.76→1.84, `protobuf` 6→7, `python-json-logger` 4.2.0, `pytest` 9, `pytest-cov` 7, `ruff` 0.16.9) y `requirements.txt` regenerado con `uv pip compile --upgrade`. `grpcio` 1.84 publica wheel `musllinux`, así que se eliminan `g++`/`linux-headers` del builder y del `Dockerfile.dev` y `libstdc++` del stage final (verificado en vivo: build, `grpcurl` a `SendOrderConfirmation`/`Health/Check`, usuario `emailservice` uid 1000, hot-reload con `watchdog` fijado a `6.0.0`). **Cobertura para CI sin ficheros de configuración auxiliares:** se descartó `python-coverage-comment-action` porque lee el `.coverage` (SQLite) y exige `relative_files = true`, que solo se puede fijar con un `.coveragerc`; en su lugar `pytest-cov` genera directamente Cobertura XML (`--cov-report=xml:reports/coverage.xml`) y lo consume `irongut/CodeCoverageSummary` (comentario en la PR + gate con `fail_below_min`), mismo patrón de "generar el formato → pasárselo a la action" que el resto.
- `recommendationservice`: Dockerfile sin usuario no-root en absoluto — corría como root, a diferencia de los demás ya revisados. Añadido el mismo patrón Alpine (`addgroup`/`adduser`) que `emailservice`, con `--chown` explícito.
- `recommendationservice`: implementación real de OpenTelemetry (no un stub) — `GrpcInstrumentorClient`/`GrpcInstrumentorServer` se instrumentaban de forma **incondicional** (solo el exportador OTLP hacia `COLLECTOR_SERVICE_ADDR` estaba condicionado a `ENABLE_TRACING`), mismo patrón que `currencyservice`/`emailservice` — eliminada por la regla 3. Profiler ya venía comentado por los propios autores (regla 2), igual que en `emailservice`.
- `recommendationservice`: `google-api-core`, `requests` y `rsa` estaban en `requirements.in` sin usarse en ningún fichero — solo servían de dependencia transitiva para poder importar `google.auth.exceptions.DefaultCredentialsError` en el bloque de tracing ya eliminado. Quitados los tres (regla 5).
- `recommendationservice`: mismo aviso de deprecación de `pythonjsonlogger.jsonlogger` → `pythonjsonlogger.json` que ya se corrigió en `emailservice` (mismo `logger.py`, casi duplicado entre ambos servicios — el propio código ya trae un `TODO` de los autores originales señalando esa duplicación).
- `recommendationservice`: `client.py` sí es un cliente de prueba válido (a diferencia del `client.js` roto de `currencyservice`) — se mantiene, mismo criterio que `AdServiceClient.java` en `adservice`.
- `checkoutservice`: mismo patrón que `productcatalogservice` — Profiler real (`cloud.google.com/go/profiler`, condicionado a `ENABLE_PROFILER`) y OpenTelemetry real con propagador/stats-handler **incondicionales** en el servidor, más un `otelgrpc.NewClientHandler()` igual de incondicional en las conexiones salientes a los otros 6 microservicios con los que habla (shipping, catálogo, carrito, currency, email, payment) — eliminados los tres (reglas 2 y 3). `initStats()` ni siquiera se llamaba desde ningún sitio, código muerto puro (regla 4).
- `checkoutservice`: `log.Level = logrus.DebugLevel` forzado sin condición — mismo hallazgo que ya se corrigió en `shippingservice` para igualar el nivel de log con `productcatalogservice` (que no lo fuerza) — quitado.
- `checkoutservice`: Dockerfile usaba `gcr.io/distroless/static` a secas — ni fijado por digest ni con el usuario `nonroot` (a diferencia de `productcatalogservice`/`shippingservice`, que sí usan la variante `:nonroot`) — corregido al mismo patrón exacto que esos dos, con `--chown`.
- `checkoutservice`: bug preexistente del código original de Google, no introducido aquí — `status.Errorf(codes.Internal, err.Error())` pasaba el error como *format string* en vez de como argumento. `go vet` (que `go test` ejecuta por defecto) lo detecta como "non-constant format string" y **rompe la compilación** — si `err.Error()` contuviera alguna vez un `%`, se interpretaría como directiva de formato. Corregido a `status.Errorf(codes.Internal, "%s", err.Error())`.
- `checkoutservice`: comando de cobertura del README corregido de `go test -coverprofile=coverage.out ./...` a la versión que excluye `genproto/` (ver regla recurrente más arriba) — comprobado en vivo: sin excluir, `go tool cover -func` listaba ~15 funciones generadas (`Reset`, `String`, `ProtoReflect`...) a 0%; con la exclusión, el informe queda limpio con solo `checkoutservice` (0%, `main.go` no tiene tests propios, solo `money`) y `money` (71.4%, real). Mismo fix aplicado retroactivamente a `productcatalogservice` y `shippingservice`, que tenían idéntica estructura `genproto/` y el mismo comando sin filtrar, nunca detectado hasta ahora.
- `checkoutservice`: **hallazgo real de `golangci-lint`, no corregido a propósito — 41 avisos `copylocks` (analizador `govet`) en `money.go`, `money_test.go` y `main.go`.** Causa: `pb.Money` es un mensaje protobuf generado, y las versiones modernas de `protobuf-go` incrustan un `sync.Mutex` en su struct interno precisamente para que `go vet` detecte cuando se copia un mensaje protobuf por valor en vez de pasarlo por puntero. Prácticamente toda la API pública de `money.go` (`IsValid`, `Sum`, `Negate`, `AreEquals`, `Must`, `MultiplySlow`...) recibe `Money` por valor — diseño original de Google, sin cambios de este refactor. `go test` no lo detecta porque el subconjunto de `go vet` que ejecuta por defecto no incluye `copylocks` (a diferencia de `printf`, que sí detectó el bug de `status.Errorf` de más arriba); `golangci-lint` sí ejecuta el `go vet` completo, de ahí que aparezca solo ahí. **Decisión: dejarlo documentado, sin tocar código** — arreglarlo de raíz implicaría cambiar la firma de casi toda la API pública del paquete (`Money` → `*Money`) y actualizar los ~15 puntos de llamada en `main.go` y los tests, un refactor real y no trivial, fuera del alcance de la checklist de validación local (no hay concurrencia real sobre esos valores hoy — es una protección defensiva de la propia librería protobuf, no un bug de carrera observado). Sirve como prueba de que el linter funciona — de hecho un hallazgo más representativo que los dejados a propósito en el resto de microservicios, porque es real y con severidad, no cosmético.
- `checkoutservice`: **cierre de la Tarea 3 — verificado también en producción, no solo con `go test`/linter en local.** `go mod tidy` sin diff (los indirect `go.opentelemetry.io/otel*` que quedan en `go.mod` son transitivos reales de `google.golang.org/grpc`, no basura de la limpieza de la regla 3 — confirmado, no hacía falta tocarlos). `docker build` de la imagen final + `docker run` con las 6 variables `*_SERVICE_ADDR` (shipping/catálogo/carrito/currency/email/payment) arranca sin error, log JSON confirma la config cargada y el puerto `5050` escuchando. Como no registra el servicio de reflection de gRPC (correcto en producción, no activado a propósito), el smoke test del *health check* (`grpc.health.v1.Health/Check`) se hace con `grpcurl -plaintext -import-path/-proto` apuntando a los `.proto` que ya están en `./protos/` del repo, en vez de depender de reflection — patrón reutilizable para probar a mano cualquier otro microservicio Go/gRPC antes de la Tarea 4 (`kind`).
- **`.gitignore` (raíz): añadido `test-report.json`.** Es la salida de `go test -json` cuando se redirige *dentro* del propio contenedor (`sh -c '... > test-report.json'`, para que caiga en el volumen del microservicio) en vez de en el host — mismo tipo de hueco de artefacto de test sin ignorar ya corregido antes para `coverage.out`/`TestResults/` (ver nota de `cartservice`), esta vez genérico a cualquier microservicio Go, no específico de `checkoutservice`.

- `adservice`: **`./gradlew run --continuous` no sirve dentro de Docker — confirmado en la práctica, no solo en teoría.** Gradle documenta que su vigilancia de ficheros no es fiable en contenedores; aquí se confirmó con un montaje `ext4` normal (sin virtualización de por medio) que el cambio llegaba al contenedor (`cat` lo mostraba) pero Gradle nunca lo detectaba ni reiniciaba. `Dockerfile.dev` usa en su lugar `watchexec --restart --poll 2s -- ./gradlew run --no-daemon`: sondeo explícito en vez de depender de inotify, mismo tipo de herramienta ya hecha que `air`/`nodemon`/`watchdog` en los demás servicios, no un script propio. Base `eclipse-temurin:...-jdk-alpine` (no `-noble`) solo para este `Dockerfile.dev` — el binario de `watchexec` (build `musl`, no `gnu`) se descarga con `wget` y se descomprime con `tar -xJ`, ambos ya incluidos en BusyBox por defecto en Alpine, sin instalar ningún paquete (`apk add`) de por medio; no afecta a la imagen de producción, que sigue en `-noble` sin cambios. Ciclo de reinicio ~30-40s (más lento que los demás porque `--no-daemon` arranca Gradle desde cero cada vez, a cambio de no depender de un daemon persistente entre reinicios).

- `frontend`: mismo patrón que `checkoutservice`/`productcatalogservice` — Profiler real (`cloud.google.com/go/profiler`, condicionado a `ENABLE_PROFILER`) y OpenTelemetry real con propagador/stats-handler **incondicionales** (`otelhttp.NewHandler` envolviendo *todas* las rutas, más `otelgrpc.NewClientHandler()` incondicional en las 7 conexiones salientes), eliminados (reglas 2 y 3). `initStats()` tampoco se llamaba desde ningún sitio, código muerto puro (regla 4). Dos loggers `logrus` distintos en este servicio (el de `main()` y el de `deployment_details.go`, usado además por los handlers vía `injectCommonTemplateData`) forzaban `log.Level = logrus.DebugLevel` sin condición — mismo hallazgo que `checkoutservice`/`shippingservice`, quitado en los dos.
- `frontend`: **integración completa del "asistente de compra" eliminada — apuntaba a `shoppingassistantservice`, el 12º microservicio ya descartado por fuera de alcance en la Tarea 2.** `shoppingassistantservice` no está en `kustomize/base/kustomization.yaml` desde entonces, así que las rutas `/assistant` (`assistantHandler`), `/bot` (`chatBotHandler`, llamaba por HTTP a `SHOPPING_ASSISTANT_SERVICE_ADDR`) y `/product-meta/{ids}` (`getProductByID`, solo consumida por el JS de `templates/assistant.html` para resolver los productos que menciona el bot) eran una IU y un cliente completos para un backend que no existe en este fork — código muerto de facto (regla 4), no un caso de "nunca activada" como el de AlloyDB sino de "activada contra un servicio que ya no está". Eliminados los tres handlers, la plantilla `assistant.html`, el enlace en `header.html` (`assistant_enabled`/`ENABLE_ASSISTANT`) y el campo `shoppingAssistantSvcAddr`. La variable `SHOPPING_ASSISTANT_SERVICE_ADDR` queda pendiente de limpiar en los manifiestos (ver nota en Tarea 8) porque esa limpieza no toca código, solo YAML.
- `frontend`: `packaging_info.go` (endpoint HTTP a un microservicio de "packaging" opcional) eliminado entero — mismo criterio que la ruta muerta de AlloyDB de `productcatalogservice`: `PACKAGING_SERVICE_URL` está comentada en los tres manifiestos que definen `frontend` (`kubernetes-manifests/`, `kustomize/base/`, `helm-chart/templates/`), nunca seteada, así que `isPackagingServiceConfigured()` siempre devolvía `false` — regla 5. El bloque `{{ if $.packagingInfo }}` correspondiente en `templates/product.html` también se quita, quedaba muerto sin esa variable en el contexto de la plantilla.
- `frontend`: `deployment_details.go` usaba `cloud.google.com/go/compute/metadata` (servidor de metadatos de GCP) para resolver `cluster-name` y `zone` y mostrarlos en el pie de página — sin equivalente en Azure/AKS y sin necesidad real (Prometheus/Grafana, Tarea 14, ya cubre esa observabilidad) — eliminado (regla 5). Se conserva solo el hostname del Pod (`os.Hostname()`), portable a cualquier distribución de Kubernetes y sin llamada de red de por medio, así que de paso se pudo quitar también el `go` que lo lanzaba en una goroutine aparte (el comentario original explicaba que era para no bloquear el arranque en despliegues no-GCP mientas esperaba el timeout de la llamada al metadata server; ya no hay llamada de red que esperar).
- `frontend`: **`homeHandler` hacía una resolución DNS real (`net.LookupHost("metadata.google.internal.")`) en cada petición a la home**, para autodetectar si corría sobre GCP y forzar el badge de plataforma a "Google Cloud" — a diferencia del resto de hallazgos de Profiler/OTel, este no es solo limpieza cosmética: es coste de red innecesario en el *literal* endpoint que golpea el test de carga de k6 (Tarea 12) en cada usuario virtual, y el propio TFM mide latencia como métrica central — dejarlo habría metido ruido sistemático (un lookup DNS fallido/con timeout en Azure) en el dato experimental. Eliminado (regla 5); el badge de plataforma sigue funcionando igual vía `ENV_PLATFORM` (ya soporta `azure` de fábrica, sin tocar `kustomize/base/frontend.yaml`, que lo deja comentado a `local` por defecto).
- `frontend`: Dockerfile de producción tenía tres problemas de la Tarea 2 en adelante sin resolver — usaba `gcr.io/distroless/static` a secas (sin fijar por digest, sin variante `:nonroot`, corría como root implícito, a diferencia de `productcatalogservice`/`shippingservice`/`checkoutservice`) y arrastraba un `ARG SKAFFOLD_GO_GCFLAGS` para `skaffold debug`, tooling ya eliminado en la Tarea 2 (`skaffold.yaml`) — código de Dockerfile muerto (regla 4). Corregido al mismo patrón exacto que los otros tres Go: digest fijado, `:nonroot`, `--chown=nonroot:nonroot` explícito también en las copias de `templates/` y `static/` (no solo el binario, regla 8), sin el ARG de Skaffold.
- `frontend`: `go mod tidy` sin diff tras la limpieza — mismos indirects `go.opentelemetry.io/otel*` que en `checkoutservice`, transitivos reales de `google.golang.org/grpc`, no hacía falta tocarlos.
- `frontend`: `golangci-lint` reporta **46 avisos `copylocks`** sobre `pb.Money` — mismo hallazgo real y documentado que los 41 de `checkoutservice` (cada microservicio Go tiene su propio `genproto.Money` generado a partir del mismo `demo.proto`, así que el problema de diseño se repite igual), con algunos más porque aquí también hay llamadas a `money.Sum`/`money.MultiplySlow`/`renderMoney` fuera del propio paquete `money` (en `handlers.go`). Misma decisión: documentado, no corregido, por la misma razón (tocaría toda la firma pública del paquete). Se corrigieron en cambio dos avisos triviales de `staticcheck` sin relación con protobuf (condición Yoda en `isCymbalBrand`, cadena `if/else if` reescrita como `switch` en `setPlatformDetails`) por ser cambios de una línea sin riesgo — quedan sin tocar 2 avisos de `errcheck` (`fmt.Fprint` sin comprobar en `/robots.txt`/`/_healthz`) — a diferencia del `errcheck` de `productcatalogservice` (ver nota más arriba, ya corregido), aquí no hay un arreglo igual de directo (no es un simple `log.Fatalf` en una goroutine de arranque, sino una respuesta HTTP ya en curso), así que se deja documentado sin tocar.
- `frontend`: **tests nuevos en `handlers_test.go`** para las funciones puras de `handlers.go` sin cobertura (`cartSize`, `cartIDs`, `stringinSlice`, `setPlatformDetails`, `renderMoney`, `renderCurrencyLogo`) — cálculo/filtrado sin red, entran de lleno en la regla 6. El resto de `handlers.go`/`main.go`/`rpc.go`/`middleware.go` se queda a 0% igual que `main.go` de `checkoutservice`, por la misma razón (exigiría un servidor gRPC real o mocks de los 7 microservicios corriente abajo, fuera del alcance de la regla 6).
- `frontend`: puerto de host remapeado en `compose.yaml` a `8082:8080` — `8080` interno choca con `emailservice` y `8081` ya lo usa `recommendationservice`. A diferencia de `paymentservice` (ver nota más arriba), aquí se mantiene el remapeo de host sin más: `frontend` no tiene una sección "Manual test request" con `grpcurl` en su README que dependa de que el puerto coincida entre desarrollo y producción (es HTTP, se prueba desde el navegador), así que no hay el mismo motivo para cambiar su puerto interno.
- `frontend`: **cierre de la Tarea 3 verificado también en producción — reproducido a mano y confirmado, no solo ejecutado una vez.** `docker build` de la imagen final construye limpio; `docker run` standalone (sin las 7 dependencias corriente abajo, apuntadas a direcciones DNS que no resuelven) arranca sin panic gracias a que `grpc.NewClient` es perezoso, `/_healthz` responde `200`, y la home responde `500` controlado (vía `renderHTTPError`) en vez de crashear — confirma que la limpieza de Profiler/OTel/asistente no dejó ninguna ruta de arranque rota. `go test`, coverage (excluyendo `genproto/`) y `golangci-lint` también reproducidos a mano con los mismos resultados documentados arriba (9.8%/71.4%/36.4% de cobertura por paquete, 48 avisos de linter = 46 `copylocks` + 2 `errcheck`).
---

## Tarea 4 — Validación local de Kubernetes (`kind`) ⏳

**Por qué va después de la Tarea 3 y no junto a ella:** desplegar la tienda completa necesita los 11 microservicios ya construidos, no tiene sentido probar el clúster con solo alguno revisado.

- Clúster local con `kind`. **Importante**: el CNI por defecto de `kind` (kindnet) *no* aplica NetworkPolicies — instalar **Cilium** (no Calico) para que el overlay hardened se comporte igual que en AKS (Tarea 7 ya fija Cilium como dataplane, no un Azure CNI genérico). `kind` tiene guía oficial para esta combinación.
- Desplegar Escenario A (`kustomize/base`): los 11 pods sanos, probes en verde.
- Desplegar Escenario B (overlay hardened): comprobar que las NetworkPolicies bloquean tráfico no autorizado de verdad (ej. `curl` a la base de datos desde un pod sin permiso debe fallar) y que el tráfico legítimo sigue funcionando.
- **Comprobar que Hubble expone `cilium_drop_count_total`** para el tráfico bloqueado del punto anterior — es la métrica de la que depende la Tarea 14 ("paquetes bloqueados por seguridad"), mejor descubrir aquí en local si no aparece como se espera que sobre Azure real.
- Comprobar que un Pod que pide más recursos de los permitidos por `LimitRange`/`ResourceQuota` es rechazado.
- Ejecución de humo del script de inyección de fallos (`kubectl delete pod ...`) y de k6 (pocos VUs, pocos segundos) contra el clúster local, antes de apuntarlos a AKS.
- (Opcional) Argo CD también instalable en el mismo `kind`, apuntando su `ApplicationSet` a los dos namespaces del propio clúster, para validar la lógica de sincronización con el mismo diseño in-cluster que se usará en AKS (ver Tarea 13) — sin RBAC/conectividad multi-clúster que validar, porque no la hay.

---

## Tarea 5 — Validación local de Terraform ⏳

- `terraform fmt -check` y `terraform validate` no necesitan cuenta de Azure con recursos.
- `terraform plan` necesita credenciales pero no crea nada — coste cero, se puede iterar el código completo revisando planes antes del primer `apply` real.

**Solo cuando las Tareas 3, 4 y 5 estén en verde se ejecuta el primer `terraform apply` contra Azure.**

---

## Tarea 6 — Pipeline de CI ⏳

**Por qué va aquí y no después de escribir Terraform/Kustomize (Tareas 7-9):** el CI no depende de que exista infraestructura en Azure, ni siquiera de que el Terraform/Kustomize definitivos ya estén escritos — se construye una vez y va creciendo de alcance solo según las Tareas 7-9 añaden contenido real:

- El **Track A** (deps, compilar, linter, tests, coverage por microservicio) solo depende de la Tarea 3 (que ya incluye escribir los tests que faltaban) — cero relación con Azure o con Terraform/Kustomize.
- `docker build` + Trivy del **Track B** ya tienen algo que validar desde ya: los 11 `Dockerfile` existen desde el fork original.
- `kustomize build` + `kube-linter` inicialmente solo validan `kustomize/base` (ya existe); en cuanto la Tarea 8 añada el overlay hardened, el mismo job empieza a validarlo también sin cambios.
- `terraform fmt`/`validate`/`plan` + tfsec/checkov/tflint inicialmente corren sobre el `terraform/` de GCP que aún no se ha sustituido; en cuanto la Tarea 7 lo reemplace por el de Azure, el mismo job (que valida por ruta, no por contenido) sigue funcionando sin tocarlo.
- La única pieza que toca Azure de verdad es `terraform plan`, y solo necesita **credenciales** (service principal/OIDC), no recursos desplegados — es de solo lectura, coste cero. Configurar esas credenciales es un trámite de una vez, no "gastar créditos".
- Lo único que de verdad gasta crédito de Azure son el pipeline de CD (Tarea 10, con su namespace de staging efímero) y el pipeline de Infra (Tarea 11, el `apply` real) — ambos ya están separados del CI.

Dos tracks en paralelo, disparados en cada Pull Request.

**Decisión de ejecución: Docker siempre que se pueda, sin sacrificar las anotaciones nativas de PR** — decidido y ya validado en la práctica durante la Tarea 3, ver esa tarea para el detalle completo del razonamiento y el patrón concreto (ejecutar con Docker → generar el formato de salida que cada action de reporting espera → pasárselo a esa action).

### A) Pipeline de aplicación (matriz por microservicio), orden fijo y su razón

1. **Checkout**
2. **Instalar dependencias** — `go mod download` / `npm ci` / `pip install -r requirements.txt` / `./gradlew dependencies` / `dotnet restore`.
3. **Compilar** — `go build ./...` / `./gradlew compileJava` / `dotnet build`. Node.js/Python no compilan (interpretados). *Va antes que el linter* porque un fallo de compilación es un corte duro más barato de diagnosticar que gastar tiempo analizando estilo de código que ni siquiera construye.
4. **Linter** (informativo, no bloqueante): Go → `golangci-lint`; Node.js (currencyservice, paymentservice) → ESLint (config `eslint.config.js` **dentro de cada microservicio**, no compartida en la raíz — decisión tomada en `paymentservice`, ver Tarea 3); Python (emailservice, recommendationservice) → `ruff`; C# (cartservice) → `dotnet format --verify-no-changes`; Java (adservice) → Checkstyle (candidata a más fricción, ver más abajo).
5. **Resultados del linter** — anotaciones inline en la PR: `golangci/golangci-lint-action` (nativo), `reviewdog/action-eslint`, `astral-sh/ruff-action` (nativo), salida directa de `dotnet format`, y para Checkstyle una acción de reviewdog si la integración nativa da problemas.
6. **Tests unitarios** (detalle completo en Tarea 3). La cobertura del paso 8 se recoge **aquí mismo**, en la misma invocación (`go test -coverprofile`, `pytest --cov`, `jest --coverage`, `dotnet test --collect:coverage` generan tests y cobertura en un único comando) — el paso 8 solo reporta ese dato, no vuelve a ejecutar nada.
7. **Resultados de tests** — unificar con [dorny/test-reporter](https://github.com/dorny/test-reporter): `golang-json` (Go, sin conversión), `dotnet-trx` (.NET), `java-junit` (JUnit XML — también sirve para el XML de `pytest --junitxml`, mismo esquema). Jest genera su JUnit vía `jest-junit`.
8. **Coverage de tests** — reporting del dato ya recogido en el paso 6. Sin acción única para las 5 tecnologías, se prueba una por lenguaje: Go → [`gwatts/go-coverage-action`](https://github.com/gwatts/go-coverage-action) (comenta el % y el diff de cobertura en la PR, self-hosted sin subir nada a terceros, y aplica el gate de umbral en el mismo paso — sustituye a la idea inicial de solo `go tool cover -func` nativo volcado a `$GITHUB_STEP_SUMMARY`, que no anotaba la PR), .NET (`coverlet` + `danielpalme/ReportGenerator-GitHub-Action`), Java (JaCoCo + `madrapps/jacoco-report`), Node/Jest (`ArtiomTr/jest-coverage-report-action`), Python (`pytest-cov` con `--cov-report=xml` + `irongut/CodeCoverageSummary`, ver nota de `emailservice` en la Tarea 3). Aviso: Gradle+JaCoCo+Actions es la combinación con más piezas móviles — si falla, se documenta como limitación en la memoria en vez de forzarla.

**Gate de cobertura, bloqueante siempre (a diferencia del linter del paso 4-5, que nunca bloquea):** el CI falla si la cobertura cae por debajo de un umbral configurado (ej. 70%), en los 5 lenguajes — no es una decisión solo de Go. Documentado aquí, en el README de cada microservicio se referencia la action sin fijar el número exacto del umbral (evita duplicar el valor en 11 sitios si se ajusta más adelante).

### B) Pipeline de infraestructura y manifiestos (no ligado a un servicio, en paralelo a A)

- `docker build` de las 11 imágenes sin push (valida Dockerfile) + Trivy sobre esas imágenes (informe, no bloquea aquí — check de PR sobre una imagen de prueba, distinto del gate de promoción a B de la Tarea 10, que corre sobre la imagen final ya publicada en ACR).
- `terraform fmt` / `validate` / `plan` (comentado en la PR) + tfsec + checkov (informe, no bloquea aquí) + `tflint` (buenas prácticas del proveedor, complementa a los dos anteriores que son de seguridad).
- `kustomize build` de los overlays A y B (valida que renderizan) + `kube-linter` sobre esa salida — detecta justo lo que mide el TFM (falta de resource limits, falta de probes, contenedores como root), generando de paso un dato comparativo extra entre A y B.
- `actionlint` sobre los propios workflows de `.github/workflows/`.

---

## Tarea 7 — Infraestructura Azure (Terraform) ⏳

- **Red privada:** VNet segmentada en subredes independientes (Ingress pública, nodos de AKS privada).
- **Un único clúster AKS compartido por A y B**, con **Azure CNI Powered by Cilium** (`--network-dataplane cilium`) y Cluster Autoscaler con mínimo/máximo de nodos. **Decisión (revisada):** se descarta un clúster por escenario — NetworkPolicy/ResourceQuota/LimitRange son recursos de namespace, no de clúster, así que separar por namespace logra el mismo aislamiento con la mitad del gasto de nodos y sin la complejidad de gestionar dos clústeres remotos. Es además el patrón real de cómo se separan tiers de seguridad en un clúster multi-tenant (ver Tarea 8). **Por qué Cilium y no el Azure NPM clásico:** ambos aplican NetworkPolicy, pero Cilium trae Hubble, que expone en Prometheus de forma nativa los paquetes descartados por política (`cilium_drop_count_total`, desglosado por namespace) — el Azure NPM clásico no da ese detalle. Es justo el dato que la Tarea 14 promete capturar ("paquetes bloqueados por seguridad"), así que la elección del dataplane no es cosmética, condiciona si esa métrica es viable o no.
- **Azure Container Registry (ACR)** — hay que provisionarlo explícitamente (no estaba dicho al principio), con autenticación desde GitHub Actions vía OIDC (`azure/login`), sin credenciales estáticas.
- **Estado remoto:** backend en Azure Blob Storage con state locking.

**Operación entre sesiones de trabajo: `az aks stop` / `az aks start`.** El clúster no se usa 24/7 (los experimentos de la Tarea 12 son manuales, `workflow_dispatch`) — parar el clúster (control plane + node pool) al terminar cada sesión y arrancarlo solo para desplegar/experimentar evita pagar cómputo mientras no se usa. Mismo criterio de coste que ya aplicado al fusionar A y B en un único clúster (más arriba), esta vez en el eje del tiempo en vez del tamaño.

---

## Tarea 8 — Dos escenarios de despliegue (Kustomize) ⏳

- **Escenario A** = `kustomize/base` tal cual (sin límites ni políticas de red), desplegado en el namespace `boutique-baseline`.
- **Escenario B** = overlay propio nuevo que activa `kustomize/components/network-policies` (ya existe) + añade `ResourceQuotas` y `LimitRanges` (no existen todavía, hay que crearlos), desplegado en el namespace `boutique-hardened`.
- **Ambos en el mismo clúster AKS** (ver Tarea 7): el namespace es la unidad de aislamiento entre escenarios, no el clúster. Cada overlay fija su propio `namespace:` en la `kustomization.yaml` para que Kustomize lo inyecte en todos los recursos sin tocar cada manifiesto a mano.

**Pendientes de manifiestos detectados durante la Tarea 3 (revisar al tocar cada manifiesto, no antes):**
- `kubernetes-manifests/productcatalogservice.yaml`, `kubernetes-manifests/paymentservice.yaml`, `kubernetes-manifests/currencyservice.yaml`, `kubernetes-manifests/emailservice.yaml` y `kubernetes-manifests/recommendationservice.yaml` setean `DISABLE_PROFILER: "1"`, variable que ya no lee ningún código (el Profiler se eliminó del fuente en la Tarea 3) — limpiar en los cinco.
- `kubernetes-manifests/frontend.yaml` (y su espejo `kustomize/base/frontend.yaml`) setean `ENABLE_PROFILER: "0"`, mismo caso que el punto anterior — limpiar. Además `SHOPPING_ASSISTANT_SERVICE_ADDR: "shoppingassistantservice:80"` queda huérfana: apunta a `shoppingassistantservice`, el 12º microservicio ya descartado por fuera de alcance en la Tarea 2, y el propio `frontend` dejó de leer esa variable al eliminarse en la Tarea 3 toda la integración del asistente de compra (rutas `/assistant`, `/bot`, `/product-meta/{ids}`, plantilla `assistant.html`) por apuntar a un servicio que no existe en este fork — limpiar la variable en ambos manifiestos, y de paso en `helm-chart/templates/frontend.yaml` si se retoma esa carpeta (ver Tarea 2, se mantiene solo de referencia).
- Añadir `securityContext: runAsNonRoot: true` (y valorar `readOnlyRootFilesystem: true`) en los pods de `productcatalogservice`, `shippingservice`, `paymentservice` y `currencyservice`, para que el manifiesto **exija** lo que sus Dockerfiles ya soportan desde la Tarea 3 (usuario no-root) — encaja de forma natural en el Escenario B (blindado), y es revisable en el resto de microservicios según cada Dockerfile vaya quedando en el mismo estado. `emailservice.yaml`, `recommendationservice.yaml`, `checkoutservice.yaml` y `frontend.yaml` ya lo traen de fábrica, no hace falta tocarlos en ese punto.

---

## Tarea 9 — Probes y autoescalado ⏳

Revisar/completar `livenessProbe`/`readinessProbe` en los manifiestos existentes y añadir Horizontal Pod Autoscaler (HPA) en el frontend.

**Interacción a vigilar en el Escenario B:** el `ResourceQuota` del namespace `boutique-hardened` (Tarea 8) puede impedir que el HPA cree las réplicas que pide si la suma ya alcanzó el límite del namespace — algo que en `boutique-baseline` (sin quota) no ocurre. No es un bug a corregir, es un dato comparativo más para la Tarea 14: el propio blindaje puede limitar la capacidad de reacción del autoescalado bajo carga.

---

## Tarea 10 — Pipeline de CD ⏳

**Secuencia al mergear a main:**

build + push de las 11 imágenes a ACR con tag = SHA del commit (artefacto inmutable, nunca se reconstruye después) + Trivy sobre esas 11 imágenes (informe, resultado reutilizado en el gate de promoción a B más abajo) → crear un **namespace de staging efímero** (`kubectl create namespace staging-<SHA>`) en el clúster AKS ya existente (Tarea 7) → deploy directo (kubectl/kustomize) + smoke test (health checks + k6 corto) → borrar el namespace de staging (pase o no) → **gate manual de aprobación** (GitHub Environment con required reviewer) → al aprobar, se actualiza el tag de imagen en el `kustomization.yaml` de A o B (commit a git): **promoción a B bloqueada si Trivy encontró alguna vulnerabilidad crítica** en alguna de las imágenes (mismo criterio A-informa/B-bloquea que tfsec/checkov en la Tarea 11); promoción a A sin bloqueo, el dato queda igualmente recogido para la comparativa → **Argo CD sincroniza automáticamente** (GitOps, pull-based; Terraform sigue push-based).

**Decisión (revisada): staging como namespace efímero, no como clúster efímero.** Al consolidar A y B en un único clúster (Tarea 7), levantar y destruir un AKS completo en cada merge a `main` (~15-20 min de aprovisionamiento) ya no tiene sentido — un namespace efímero en el mismo clúster da el mismo smoke test en segundos, sin `terraform apply`/`destroy` de por medio. Es también más consistente con haber descartado los entornos efímeros por PR "por complejidad/coste desproporcionado" (ver "Decisiones descartadas explícitamente"): un clúster efímero por cada merge era, sin darse cuenta, más pesado que lo que ya se había descartado para las PRs.

**Gate de Trivy (imágenes) — mismo criterio que el de tfsec/checkov (Terraform, Tarea 11), aplicado en el punto correcto del pipeline correcto:** el escaneo de Trivy en la Tarea 6 (PR) es solo informativo y anterior al build final — no sustituye a este gate. Este, en cambio, corre sobre la imagen ya construida e inmutable, y decide si esa imagen concreta puede promocionarse a B — no se reconstruye nada, solo se bloquea o no el `commit` que actualiza el tag en el overlay hardened.

**Piezas añadidas tras revisar "despliegue/docker" (no quitan nada de lo anterior):**
- **Gate manual (GitHub Environment con required reviewer) confirmado viable:** esta función requiere plan Pro/Team/Enterprise en repos privados, pero es gratuita en repos públicos — el repo es y seguirá siendo público (es un fork), así que no hay restricción de plan que afecte a este pipeline.
- **ACR** provisionado en la Tarea 7 (Infra), no en el propio CD.
- **Firma de imagen + SBOM**: firmar con `cosign` y generar un SBOM (Syft o atestación nativa de `docker buildx`) — barato y muy on-tema para un TFM de seguridad. Opcional pero recomendable. **Si se implementa, tiene que incluir verificación, no solo firma:** firmar sin verificar en ningún punto del pipeline no aporta seguridad real, solo trazabilidad para auditoría manual — hace falta un paso que rechace imágenes no firmadas (`kyverno` con política `verifyImages`, o `cosign verify` antes de que Argo sincronice), con el mismo criterio A-informa/B-bloquea que ya se usa para tfsec/checkov y Trivy.
- **Ingress público**: falta un Ingress Controller real (nginx-ingress o AGIC de Azure) + `cert-manager` si se quiere TLS, para exponer el frontend. Se despliega igual que el resto de la app, vía Argo.
- **Rollback**: con GitOps es solo un `git revert` del commit que cambió el tag de imagen — Argo CD resincroniza automáticamente al estado anterior, sin mecanismo adicional.
- **Salud del despliegue**: Argo CD puede esperar a que los pods estén `Ready` (usando las mismas probes de la Tarea 9) antes de marcar la sincronización como `Healthy`.

---

## Tarea 11 — Pipeline de Infra (trigger: cambios en `terraform/`) ⏳

Aplica VNet, el clúster AKS compartido, ACR y backend remoto (Tarea 7). tfsec/checkov es **bloqueante únicamente en el plan/apply del Escenario B** (hardened); en A solo informa, generando el dato para la comparativa de vulnerabilidades permitidas vs bloqueadas — es literalmente el eje comparativo del TFM.

---

## Tarea 12 — Pipeline de Experimento (`workflow_dispatch`, desacoplado de los deploys) ⏳

Contenedor k6 (carga + perfil "usuario testigo") junto con el script de inyección de fallos (Bash/kubectl) que elimina el pod de un microservicio crítico a mitad de carga. Ejecutado contra A y luego contra B, repetible sin necesidad de redesplegar. Deliberadamente desacoplado del CD para poder repetir mediciones sin tocar código.

**k6 corre in-cluster, nunca a través del Ingress público.** El Job de k6 apunta al `ClusterIP`/DNS interno del frontend de cada namespace (`frontend.boutique-baseline.svc.cluster.local` / `frontend.boutique-hardened.svc.cluster.local`), no a la URL pública. Medir a través de Ingress/DNS/TLS metería latencia de esa capa de red en el dato que es el resultado central del TFM. El Ingress público (Tarea 10) es solo para navegar la tienda manualmente en una demo, no forma parte de la medición.

**Protocolo de ejecución, obligatorio al compartir un único clúster (Tarea 7) entre A y B:**
- **Nunca correr A y B en paralelo.** El Cluster Autoscaler es compartido: si el test de A escala nodos, lanzar B inmediatamente después arrancaría sobre un clúster ya "caliente" con más nodos de los que tendría en frío, sesgando la comparación de consumo/latencia.
- **Esperar el cooldown del autoscaler entre runs** y confirmar `kubectl get nodes` vuelve al mínimo antes de arrancar el otro escenario.
- **Filtrar las métricas de Prometheus por namespace** (`container_cpu_usage_seconds_total{namespace="boutique-baseline|boutique-hardened"}`, vía cAdvisor/kube-state-metrics), no usar el consumo de nodo en crudo — en un clúster compartido, el nodo puede reflejar restos de ambos escenarios si el cooldown no fue completo.

**Rigor estadístico: N≥3 repeticiones por escenario, no una sola ejecución.** Una única corrida de k6 por escenario es un solo punto de datos — no permite distinguir una diferencia real de A vs B del ruido de una ejecución puntual. Repetir cada escenario al menos 3 veces (mismo perfil de carga, mismo cooldown entre runs) y reportar en Grafana media/mediana y desviación de consumo, latencia y MTTR, no un valor único. El microservicio objetivo de la inyección de fallos debe ser **fijo**, no aleatorio, para que las repeticiones de A y B sean comparables entre sí.

---

## Tarea 13 — GitOps con Argo CD ⏳

**Decisión confirmada: Argo CD, no Flux, no ambos.**

- **Por qué Argo y no Flux:** tiene UI web (vistoso para la defensa del TFM: tiles Synced/Healthy en directo), y `ApplicationSet` encaja perfecto con el caso de uso (misma app, overlays distintos — base para A, hardened para B — desplegados en namespaces distintos del mismo clúster desde una única definición). Reconocimiento de mercado ligeramente mayor que Flux también cuenta para el CV. Flux es más ligero en recursos y su soporte nativo de Kustomize es más "purista", pero para una defensa académica la UI de Argo inclina la balanza.
- **Por qué no los dos:** sería complejidad añadida sin aportar nada a la pregunta de investigación real.
- **Diseño (revisado): Argo CD instalado in-cluster (self-managed), no en un hub separado.** Al consolidar A y B en un único clúster AKS (Tarea 7) ya no hace falta un clúster adicional solo para alojar el control plane de Argo, ni registrar clústeres remotos con su propio RBAC/conectividad — Argo gestiona sus Applications en el mismo clúster donde corre. Un `ApplicationSet` con generador de lista (matriz namespace × overlay) despliega `kustomize/base` en `boutique-baseline` y el overlay hardened en `boutique-hardened`. El "deploy" pasa a ser solo un commit en git (Tarea 10); Argo sincroniza solo. Terraform sigue siendo push-based (Argo no gestiona infra de nube).
- **Nota sobre Helm (aclaración recurrente):** el CLI `helm` sí se usa — es la forma estándar de instalar Prometheus/Grafana (`kube-prometheus-stack`) y el propio Argo CD (su chart oficial), normalmente vía el provider de Helm de Terraform. Esto es distinto de la carpeta `helm-chart/` del repo (que empaqueta la app Online Boutique en sí y no se usa, ver Tarea 2).

---

## Tarea 14 — Observabilidad (Prometheus + Grafana) ⏳

- **Captura de métricas:** consumo real de recursos por namespace/pod (no de nodo en crudo, ver protocolo de la Tarea 12), latencias de red, tasas de error HTTP/gRPC de la app y paquetes bloqueados por seguridad (vía Hubble/Cilium, ver Tarea 7).
- **Dashboard de laboratorio:** MTTR (Mean Time To Recovery) — segundos exactos que tarda Kubernetes en levantar un pod nuevo tras una simulación de fallo.
- **Usuario testigo:** perfil de usuario independiente en k6 que realiza una compra simulada de forma constante y pausada mientras el resto de usuarios virtuales estresan el sistema, para medir con precisión cuántos segundos exactos experimenta un cliente real la web caída o lenta durante la inyección de fallos.

---

## Mejoras opcionales (si sobra tiempo, no comprometidas)

- **Postgres real en Azure para `productcatalogservice`.** El servicio es puramente de lectura (`ListProducts`/`GetProduct`/`SearchProducts`, sin ningún CRUD de escritura), así que conectarlo a una base de datos gestionada de Azure en vez de `products.json` sería una mejora de infraestructura sin fricción añadida (no hay que resolver consistencia de escrituras concurrentes ni nada parecido). Puramente cosmético/demostrativo, no aporta a la pregunta de investigación — no bloquea nada si no se hace.
- **Descartado a propósito, no reconsiderar:** hacer lo mismo para el carrito (`cartservice`) — el carrito se identifica por una cookie de sesión efímera, no hay sistema de cuentas/autenticación en el proyecto, así que no hay identidad de usuario persistente a la que unir un carrito guardado en base de datos. Redis (session store en memoria con expiración) es el diseño correcto para ese caso, no una limitación a resolver.

---

## Trabajo futuro / limitaciones a mencionar en la memoria final

- **Cifrado en tránsito entre microservicios (TLS/mTLS).** Todo el tráfico gRPC interno usa credenciales `insecure` (texto plano) — decisión consciente, no descuido. El eje de seguridad medido en este TFM es **aislamiento de red** (NetworkPolicies, Escenario A vs B), no cifrado en tránsito; son capas complementarias pero solo la primera está en el alcance declarado. Hacerlo bien (mTLS con rotación automática de certificados entre 11 microservicios en 5 lenguajes distintos) requeriría un *service mesh* tipo Istio — descartado explícitamente por complejidad desproporcionada para un TFM en solitario (ver Tarea 13). Mencionar en la memoria como limitación conocida y justificada, no como algo pendiente de arreglar.
- **Gestión de secretos (Azure Key Vault / CSI Secrets Store Driver / External Secrets Operator).** No hay ninguna pieza de gestión de secretos en el alcance porque la app base no tiene secretos reales que gestionar (ni contraseñas de base de datos, ni API keys) — el único acceso a Azure con credenciales es GitHub Actions vía OIDC (Tarea 7), que ya evita credenciales estáticas. Si en algún momento se implementa la mejora opcional de Postgres para `productcatalogservice`, esa cadena de conexión debe salir de Key Vault (vía CSI driver) y no ir en claro en un manifiesto — anotado aquí para no perderlo de vista si esa mejora se retoma, no como algo pendiente de arreglar hoy.

---

## Decisiones descartadas explícitamente (no reconsiderar sin motivo nuevo)

- **Entornos efímeros por Pull Request** — descartado por complejidad/coste de infraestructura (namespace dinámico, DNS/ingress temporal, gestión de credenciales) desproporcionado para un TFM en solitario. El mismo efecto de "ver el cambio antes de producción" se consigue con el staging único de la Tarea 10.
- **Proceso de releases versionado formal** (semantic-release, changelog automático) — no aporta al objetivo científico. La reproducibilidad académica se cubre con `git tag` simples al congelar cada escenario (ej. `v1.0-baseline`, `v1.0-hardened`).
- **Flux en vez de (o además de) Argo CD** — ver Tarea 13.
- **Copiar solo `src/` a un repo nuevo** — ver Tarea 1.

---

## Auditoría de justificación de tecnologías

| Tecnología | Justificación | Fuerza |
|---|---|---|
| Terraform + backend remoto | Requisito explícito del TFM (IaC, VNet segmentada, AKS) | Alta |
| GitHub Actions (CI+CD+Infra+Experimento) | Requisito explícito del TFM | Alta |
| tfsec/checkov/Trivy diferenciado A vs B | Es literalmente el eje comparativo del TFM (shift-left) | Alta |
| k6 + script de inyección de fallos | Es el experimento central del TFM | Alta |
| Prometheus + Grafana + MTTR | Requisito explícito del TFM | Alta |
| Staging efímero + build-once-promote-artifact | Protege la integridad de las mediciones de A/B frente a errores de despliegue | Alta |
| Gate manual (GitHub Environment) | Coste ~10 min de configuración, simula control real de producción | Media-alta |
| **Argo CD / GitOps** | **No afecta al resultado experimental** (MTTR, latencia y NetworkPolicies dan igual si el manifiesto llega por `kubectl apply` desde CI o por sincronización de Argo). Su valor real es narrativo/CV (UI vistosa, disciplina build-once-promote). A cambio añade superficie real: `ApplicationSet` que aprender — instalado in-cluster (ver Tarea 13), ya no hace falta RBAC/conectividad multi-clúster. | **Media — la única pieza "nice to have", no "necesaria para la ciencia"** |

Si el tiempo aprieta más adelante, Argo CD es la primera pieza recortable sin invalidar ningún resultado de la comparativa A/B.

---

## Verificación / salud continua

- Tras cada fase, comprobar que `kubectl apply -k kustomize/` (o el overlay que corresponda) sigue desplegando los 11 servicios sin errores antes de pasar a la siguiente.
- Verificar que ningún workflow de `.github/workflows/` restante referencia un fichero borrado (`grep -rl` de los nombres borrados sobre `.github/` y `docs/`).
