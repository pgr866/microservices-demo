# Local development only. Watches .py files and restarts on change.
FROM python:3.14.7-alpine
WORKDIR /recommendationservice
RUN apk add --no-cache g++ linux-headers
COPY requirements.txt .
RUN pip install -r requirements.txt watchdog
ENTRYPOINT ["watchmedo", "auto-restart", "--directory=.", "--pattern=*.py", "--recursive", "--", "python", "recommendation_server.py"]
